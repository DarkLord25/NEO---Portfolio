{
  "name": "portfolio-neo",
  "private": true,
  "workspaces": ["spline-hero"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev -w spline-hero",
    "build": "npm run build --prefix spline-hero",
    "preview": "npm run preview -w spline-hero"
  }
}
